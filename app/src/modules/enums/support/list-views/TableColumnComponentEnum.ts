export enum TableColumnComponentEnum {
    DATE = 'date',
    TIME = 'time',
    CURRENCY = 'currency',
    BOOLEAN = 'boolean',
    BADGE = 'badge',
    ENUM = 'enum',
    IMAGE = 'image',
    WEEKDAY = 'weekday',
    COMPONENT_PREVIEW = 'component-preview',
    SKILL_LABEL = 'skill-label',
    MULTI_SELECT = 'multi-select',
    CUSTOM = 'custom',
}
