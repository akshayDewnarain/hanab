/**
 * Scalar values allowed in static select option lists and value maps.
 *
 * Used when options are expressed as plain strings or numbers instead of rich `{ value, label }` rows.
 */
export type Primitive = string | number;
