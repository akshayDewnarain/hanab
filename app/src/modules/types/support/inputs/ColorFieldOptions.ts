import type { BaseInputFieldOptions } from '@/modules/types/support/inputs/BaseInputFieldOptions.ts';

/** Options for a hex color field with native color picker + text input. */
export type ColorFieldOptions = BaseInputFieldOptions & {};
