import type { BaseInputFieldOptions } from '@/modules/types/support/inputs/BaseInputFieldOptions.ts';

/**
 * Options for a single-line text input.
 *
 * Uses {@link BaseInputFieldOptions} for all keys; no additional properties on top of the base shape.
 */
export type InputFieldOptions = BaseInputFieldOptions & {};
