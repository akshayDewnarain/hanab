/**
 * One bullet in the optional “how this works” callout on {@link SimpleGroupedRelationPicker}.
 *
 * @property icon - Iconify icon id.
 * @property textKey - i18n key for the tip body.
 */
export type CategoryGroupedRelationHowToTip = {
    icon: string;
    textKey: string;
};
